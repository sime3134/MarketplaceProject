package org.marketplace.server.model;

/**
 * Enum class used to specify the different messagetypes.
 */

public enum MessageType {
    INFO,
    ERROR,
    SUCCESS,
    ORDER_STATUS_NOTIFICATION,
    PURCHASE_NOTIFICATION, SUBSCRIPTION_NOTIFICATION,
}
